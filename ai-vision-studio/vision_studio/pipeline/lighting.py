from typing import Any


def correct_lighting(image: Any, cfg: Any = None) -> tuple[Any, dict]:
    """Correct lighting, white balance, and contrast (Phase 1 stub)."""
    return image, {"stage": "lighting", "status": "skipped_stub"}
