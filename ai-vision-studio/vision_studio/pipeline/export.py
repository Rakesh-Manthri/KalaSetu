from typing import Any


def export_image(image: Any, cfg: Any = None) -> tuple[Any, dict]:
    """Export formatted image with compression and metadata (Phase 1 stub)."""
    return image, {"stage": "export", "status": "skipped_stub"}
