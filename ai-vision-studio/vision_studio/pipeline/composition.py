from typing import Any


def compose_product(image: Any, cfg: Any = None) -> tuple[Any, dict]:
    """Center, pad, resize and add background color for e-commerce standards (Phase 1 stub)."""
    return image, {"stage": "composition", "status": "skipped_stub"}
